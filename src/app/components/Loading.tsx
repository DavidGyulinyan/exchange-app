import { Typography } from "@mui/material";

export default function Loading() {
  return (
    <div>
      <Typography variant="h6">Preparing the currency converter...</Typography>
    </div>
  );
}
